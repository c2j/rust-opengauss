[package]
name = "opengauss-derive-test"
version = "0.1.0"
authors = ["Steven Fackler <sfackler@gmail.com>"]
edition = "2024"
rust-version = "1.85"

[features]
## Gates integration tests requiring a live DB (127.0.0.1:5433 via docker compose).
integration = []

[dev-dependencies]
trybuild = "1.0.18"

opengauss-types = { path = "../../crates/opengauss-types", features = ["derive"] }
opengauss = { path = "../../crates/opengauss" }
